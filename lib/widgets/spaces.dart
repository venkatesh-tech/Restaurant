import 'package:flutter/material.dart';
import 'package:restaurant/values/values.dart';

class SpacesHeight4 extends StatelessWidget {
  const SpacesHeight4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 4.0,
    );
  }
}

class SpacesHeight8 extends StatelessWidget {
  const SpacesHeight8({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 8.0,
    );
  }
}

class SpacesHeight12 extends StatelessWidget {
  const SpacesHeight12({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 12.0,
    );
  }
}

class SpacesHeight16 extends StatelessWidget {
  const SpacesHeight16({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(
      height: 16.0,
    );
  }
}
